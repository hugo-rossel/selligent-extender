name: Package and Release Selligent EXTender

on:
  push:
    tags:
      - 'v*' # Se déclenche lors du push d'un tag de version (ex: v1.4)

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Package Extension (Prod Only)
        run: |
          zip -r selligent-extender.zip ./* -x "README.md" -x ".git*" -x ".github*"

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          files: selligent-extender.zip
          draft: false
          prerelease: false
          name: Selligent EXTender ${{ github.ref_name }}
          body: |
            ## Selligent EXTender ${{ github.ref_name }} (Production)
            
            ### 🌐 Page d'accueil & Guide de mise à jour
            Retrouvez le guide complet d'installation et de mise à jour sur la page d'accueil du dépôt :
            👉 **[https://github.com/hugo-rossel/selligent-extender](https://github.com/hugo-rossel/selligent-extender)**
            
            ### 🔄 Comment mettre à jour :
            1. Téléchargez le fichier **`selligent-extender.zip`** ci-dessous.
            2. Extrayez son contenu et remplacez les fichiers existants dans votre dossier d'installation permanent (ex : `C:\SelligentExtender`).
            3. Ouvrez **`chrome://extensions/`** dans Chrome et cliquez sur l'icône **Actualiser 🔄** sur la carte de l'extension *Selligent EXTender*.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
